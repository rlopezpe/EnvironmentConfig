<VirtualHost *:80>
    DocumentRoot "<put the project root path here, the rest you can leave as is>"

    ServerName environmentConfig.local
    ServerAlias ec.local
    ServerAlias environmentConfig.dev
    ServerAlias environmentConfig.staging
    ServerAlias ec.production
    ErrorLog "logs/environmentConfig.local-error_log"
</VirtualHost>
